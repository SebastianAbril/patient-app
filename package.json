{
    "name": "patient-app",
    "version": "0.0.1",
    "engines": {
        "node": "16.x"
    },
    "scripts": {
        "postinstall": "npm ci --prefix backend && npm ci --prefix frontend",
        "build": "npm run build --prefix frontend"
    }
}
